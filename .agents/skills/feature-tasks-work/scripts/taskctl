#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
if command -v bun >/dev/null 2>&1; then
  exec bun "$SCRIPT_DIR/taskctl.mjs" "$@"
fi

echo "taskctl: Bun is required to run taskctl.mjs." >&2
echo "Install Bun: https://bun.sh" >&2
exit 1
