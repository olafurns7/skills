interface:
  display_name: "Feature Tasks Work"
  short_description: "Orchestrate task execution via delegation."
  default_prompt: "Use $feature-tasks-work to orchestrate work from planning/<title-slug>/SPEC.md and planning/<title-slug>/tasks.yaml with delegated execution."
